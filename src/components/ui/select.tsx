// Select placeholder for React Native
import React from 'react';
import { View, Text } from 'react-native';

export function Select(props: any) {
  // TODO: Integrate with a native picker (e.g. react-native-picker-select)
  return (
    <View style={{ padding: 8 }}>
      <Text style={{ color: '#0081A7' }}>Select (próximamente)</Text>
    </View>
  );
}
