// Calendar placeholder for React Native
import React from 'react';
import { View, Text } from 'react-native';

export function Calendar(props: any) {
  // TODO: Integrate with a native calendar library (e.g. react-native-calendars)
  return (
    <View style={{ padding: 16, alignItems: 'center' }}>
      <Text style={{ color: '#0081A7', fontWeight: 'bold' }}>Calendario (próximamente)</Text>
    </View>
  );
}
